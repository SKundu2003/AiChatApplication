package com.AiChatApplication.service;

import org.springframework.ai.chat.model.ChatResponse;

public interface ChatService {
    String chat(String message);

    ChatResponse generateResponseOptions(String message);
}
